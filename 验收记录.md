# 验收记录

2026-09-16，提交目录内实测：五项测试全部通过；日志展示复测五项通过；默认离线演示退出码 0。

90000 使用真实 handler、真实三秒延迟和两秒超时，返回 TIMEOUT_UNKNOWN，两端余额不变。审批前打印 CONFIRM；审计目标显示 ACC-A-****3456 与 ACC-A-****4321。权限引擎 AST 与新附件一致。详细输出见验收输出.txt。
